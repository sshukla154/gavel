# Test-profile overrides only — the main application.yaml still loads underneath.
spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: http://localhost:9999/mock-jwks
  kafka:
    listener:
      auto-startup: false
