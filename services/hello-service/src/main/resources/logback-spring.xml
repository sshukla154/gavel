<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- ── Local / default profile: human-readable console output ─────────── -->
    <springProfile name="!prod">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
            </encoder>
        </appender>

        <logger name="com.shukla.gavel" level="DEBUG"/>

        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

    <!-- ── Production profile: JSON structured logging (ECS) via application-prod.yaml ── -->
    <!-- Spring Boot 4 logging.structured.format.console=ecs takes over in prod;        -->
    <!-- this block is a no-op safety net so Logback does not fall back to defaults.    -->
    <springProfile name="prod">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
            </encoder>
        </appender>

        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

</configuration>
