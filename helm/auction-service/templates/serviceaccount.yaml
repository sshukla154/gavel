{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "auction-service.serviceAccountName" . }}
  labels:
    {{- include "auction-service.labels" . | nindent 4 }}
{{- end }}
