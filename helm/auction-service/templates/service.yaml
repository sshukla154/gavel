apiVersion: v1
kind: Service
metadata:
  name: {{ include "auction-service.fullname" . }}
  labels:
    {{- include "auction-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "auction-service.selectorLabels" . | nindent 4 }}
