{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "bid-service.serviceAccountName" . }}
  labels:
    {{- include "bid-service.labels" . | nindent 4 }}
{{- end }}
